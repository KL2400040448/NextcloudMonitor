## 🧠 Nextcloud Server Connectivity Test

This PowerShell script checks if the Nextcloud server is reachable and if its web service is responding.

### Run the script:
```powershell
.\NextcloudMonitor.ps1
